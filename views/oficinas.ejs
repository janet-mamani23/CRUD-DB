<!DOCTYPE html>
<html>

<%- include('./head.ejs') %>

    <body>
        <%- include('./navigation.ejs') %>
            <h1> <%= title %> </h1>
            <form action="/agregarOficina" method="post">
                <input type="text" name="denominacion" placeholder="Oficina">
                <button type="submit">Agregar</button>
            </form>
            <div class="container">
                <% oficinas.forEach(element => { %>
                    <div class="row">
                        <div class="col-md-4 mt-4">
                            <%= element.denominacion %>
                        </div>
                        <div class="col-md-4 mt-4">
                            <a class="btn btn-primary" href="/editOficina/<%= element.id %>" role="button">Editar</a> 
                            <a class="btn btn-primary" href="/deleteOficina/<%= element.id %>" role="button">Borrar</a>
                        </div> <!--enlaces de botones, cuando se hacen clic, llevan al usuario a las páginas donde pueden editar o borrar una oficina específica, dependiendo del ID proporcionado en element.id.-->
                    </div>
                    <% }) %>

            </div>


            <ul>

            </ul>
    </body>

</html>
